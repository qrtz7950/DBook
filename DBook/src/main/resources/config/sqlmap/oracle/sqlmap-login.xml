<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.project.login.dao.LoginDAO">
	
	<!-- 회원가입 -->	
	<insert id="signIn" parameterType="loginVO">
		insert into db_user(no, id, profile_image, thumbnail_image, nickname, age_range, gender)
		 			values(seq_db_user_no.nextval, #{id}, #{profile_image}, #{thumbnail_image}, #{nickname}, #{age_range}, #{gender})
	</insert>
	
	<!-- 회원가입체크 -->
	<select id="overlapCheck" resultType="loginVO" parameterType="loginVO">
		select id from db_user where id = #{id}
	</select>
	
 	<update id="update" parameterType="loginVO">
		update db_user 
			set		profile_image = #{profile_image}
				,	thumbnail_image = #{thumbnail_image}
				,	nickname = #{nickname}
				,	age_range = #{age_range}
				,	gender = #{gender}
			where 	id = #{id}
	</update>
</mapper>